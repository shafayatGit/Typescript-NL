//JS & TS --> string, number, boolean, null, undefined
//TS --> never, unknown, void

let userName : string = "Shafayat"; // string
//userName = 12; //! Error -> can't assign number to string type

let age = true; //Define na kore dile type infer hoye jay boolean
let age2 : boolean = false; //Explicitly boolean type

let x; //any type
let y : undefined; //undefined type and we can not assign anything in undefined type except undefined
let z : null; //null type and we can not assign anything in null type except null